#include <bits/stdc++.h>

using namespace std;

long long guard(int day){
    int dp_guard[day];
    int dp_pow4[day];


}

int main() {
    int T,temp;
    cin >> T;
    for (int i = 0; i < T; ++i) {
        cin >> temp;
        cout << guard(temp) << "\n";
    }
    return 0;
}
