#include <bits/stdc++.h>

using namespace std;

int cutter(int arr[],int n){
    int dp[n+1][n+1];
}


int main() {
    int L, n;
    cin >> L >> n;
    int pos[n];
    for (int i = 0; i < n; ++i) {
        cin >> pos[i];
    }
    cutter(pos,n);
    return 0;
}
