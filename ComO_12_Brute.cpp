#include <bits/stdc++.h>

using namespace std;

int maxDamage(int arr[8][8]) {
    int max;
    vector<int> ans;
    for (int i = 0; i < 8; ++i) {
        for (int j = 0; j < 8; ++j) {
            for (int j = 0; j < 8; ++j) {
                if(isSave){

                }
            }

        }
    }
}

int main() {
    int t, map[8][8];
    cin >> t;
    for (int i = 0; i < t; ++i) {
        for (auto &j : map) {
            for (int &k : j) {
                cin >> k;
            }
        }
    }
    return 0;
}
